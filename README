The API uses MYSQL database so you should provide the connection parameters and run the test.py to create the database and tables.
To insert all the rows might take a lot of time. So you can import the csv after you create the tables. 
The table definition is provided in the test.py.
Without the database the Doctor search and Insurance Company search will not work.
Run the React code and start the chrome browser and disable web security

press windows+R type in this command and go to localhost:3000
chrome.exe --user-data-dir="C://Chrome dev session" --disable-web-security
